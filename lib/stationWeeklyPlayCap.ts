/**
 * Нэг тоглоом / нэг өртөө — rolling 7 хоногийн цонхонд хамгийн ихдээ хэдэн удаа
 * `completeGame` тоолно. Сервер `server/src/stationWeeklyPlayCap.ts`-тай ижил байх ёстой.
 */
export const STATION_GAME_WEEKLY_PLAY_CAP = 3;
